# Questions for Supervisor

Use this file to collect questions that should be confirmed before final submission.

For a short sendable metadata/approval request, see `docs/supervisor_handoff_request.md`.

For the current technical answers to the 3x3 topic-brief requirements, see
`docs/3x3_thesis_questions_answered.md`. The scope questions below should now be
treated mainly as supervisor approval/wording questions, not as proof that the
repository lacks a 3x3 implementation answer.

## Formatting and submission

1. The official ECE diploma-theses page links Greek and English Word templates, now archived as `specs/ece_diploma_template_el.doc` and `specs/ece_diploma_template_en.doc`; the LaTeX first pages now follow their cover/certification/abstract structure. Is this adaptation sufficient for review, or must the Word-template wording/layout be reproduced exactly?
2. The LaTeX front matter now uses the extracted Greek template wording for the originality/copyright and certification pages. Is that wording acceptable as-is after final metadata is filled, or is there a newer supervisor/Secretariat wording requirement?
3. Should the thesis include the university/department logo, and if yes, which official file should be used?
4. Which bibliography style is preferred?
5. Are there page-count expectations or limits beyond the department's general thesis rules?
6. Is English terminology acceptable in parentheses after Greek technical terms?
7. The verified ECE sources require Greek writing with an extended English abstract and public presentation after committee submission. Is the current Greek/English abstract structure acceptable for review?
8. Which student name format, registration number, committee members/titles, examination date, declaration text, and signature blocks must be added before final submission?
9. The ECE deposit page currently says the final deposit needs the supervisor's grade-sheet submission, the final thesis in PDF/Word on CD, and the Nemertes protocol number, with no printed thesis copy. Are these still the exact steps for this submission?

### Front-matter fields currently detected as placeholders

The audit script writes these unresolved fields to `results/processed/thesis_audit.json` under `front_matter_placeholders`, groups them under `submission_blockers`, and sets `final_submission_ready: false` until they are replaced by authoritative values. The same collection list is kept in `docs/final_metadata_packet.md`. A separate approval-evidence blocker remains until `docs/final_supervisor_approval.template.md` is copied to `docs/final_supervisor_approval.md` with the real approval source/date and approved AI/style/scoped-claim decisions.

- Greek student display name;
- English student display name;
- full student name with patronymic;
- registration number;
- place/month/year line;
- copyright year;
- public examination date;
- two committee member names/titles/departments;
- division director name/title/signature details.

## Licensing and provenance

The repository now includes `LICENSE` and `THIRD_PARTY_NOTICES.md`. These files record that the original repository has no project-wide license yet, that vendored `native/h48_backend/third_party/nissy_core/` is GPL-3.0-or-later with local license text and code-recorded commit `3cb60bcbf4ab9af4e9452a43681f1e7176b0c88f`, that public Nissy tables are sourced from `https://nissy.tronto.net/nissy-tables-2.0.4.zip` with recorded CRC/size evidence, and that RubikOptimal is used only as an installed external backend/table cache, not vendored source. Please confirm:

- Is GPL-3.0-or-later vendored nissy-core source acceptable inside the submitted thesis repository if attribution, local license path, source URL, commit, and non-originality boundaries remain explicit?
- Should the submitted package include the ignored `.codex_external/` Nissy/RubikOptimal caches and generated large table binaries, or should those remain reproducible/local artifacts excluded from the final handoff?
- Is the thesis wording acceptable if H48/Nissy/RubikOptimal are described as public-solver-derived exact-backend evidence while native Korf/IDA* remains the student heuristic/implementation contribution?
- Is the added front-matter originality paragraph acceptable, specifically the statement that thesis-owned work covers the repository organization, local modelling/checking/experiment infrastructure, native Kociemba/Thistlethwaite/Korf paths, and analysis, while vendored GPL `nissy-core`/H48, public Nissy/RubikOptimal backends, and tables generated from those public components are attributed third-party/public-solver-derived evidence rather than original algorithmic authorship?
- Should the final submission baseline track generated binary table files in Git, use Git LFS, attach them as an external artifact archive, or require regeneration from scripts only? The current safe runbook treats this as an approval decision because `data` and `results` are multi-GiB directories and the local checkout has no committed source baseline yet.

## Scope and algorithms

1. Is a strong Python implementation preferred over a mixed Python/Prolog implementation?
2. Should Prolog be included because the topic brief references Prolog material, or is Python sufficient?
3. Is the current native Kociemba-style two-phase implementation sufficient as the main practical 3x3 solver if it verifies all fixed thesis-profile cases but is clearly not claimed globally optimal?
4. Is the current Thistlethwaite implementation acceptable as an educational four-phase subgroup-chain solver without the historical static maneuver tables, or is a closer maneuver-table reproduction required?
5. Is the current 3x3 end-to-end sequence/facelet workflow acceptable as practical functionality evidence, given that it verifies solutions but does not prove optimality?
6. Is the current row-scoped native optimal 3x3 evidence acceptable: a compiled full-cube IDA* solver using complete corner and 6-edge PDBs, with exact claims only for cases where the search completes?
7. Is it acceptable to include Nissy-light as an external optimal reference backend and candidate upper-bound source for stress evidence, clearly separated from the in-repo native implementation claims?
8. Are the complete native 3x3 corner pattern database (`8! * 3^7` states), eight complete full-cost 6-edge PDBs (`42,577,920` states each), and the first two complete URF/DLB cost-partitioned edge CPDBs sufficient for the Korf/IDA* thesis track, given that the saved CPDB coverage artifact records no sampled improvement beyond the eight-PDB max?
9. The repository now vendors GPL-3.0-or-later nissy-core H48 source and wraps it as an in-repository backend with generated H48 h0 and oracle-grade h48h7 table metadata/checksums. Is this acceptable for the thesis repository if attribution, license, citations, and non-originality limits are explicit?
10. The deterministic depth-20 stress row is exact at distance 17 under the in-repository H48 backend in both h48h0 and h48h7 artifacts, the H48 path accepts direct cubie/facelet state input, `results/processed/optimal_3x3_seed_2026_stress_h48h7_oracle.json` proves 15/15 direct-state rows exact including 10 extra deterministic depth-25 states, `results/processed/h48_oracle_certification_seed_2026_thesis.json` proves the standard superflip state exact at distance 20, `results/processed/h48_oracle_certification_seed_2026_thesis_trusted_no_preload.json` proves the trusted/no-preload hard certification corpus exact with max runtime 91.629342s, `results/processed/h48_batch_overhead_seed_2026_thesis.json` shows 11.56x throughput speedup for 12 repeated h48h7 exact calls using batch table loading, `results/processed/h48_trusted_table_speedup_seed_2026_thesis_h48h7.json` shows 189.894x checked-versus-trusted speedup for repeated shallow calls after table generation, `results/processed/h48_resident_oracle_seed_2026_thesis_h48h7_trusted.json` shows a resident native-process speedup of 40.16x on 8 repeated h48h7 trusted exact calls, `results/processed/h48_oracle_stream_seed_2026_thesis_trusted.json` proves the public resident `rubik-optimal oracle --stream` interface on solved, sequence, and facelet inputs, `src/rubik_optimal/oracle.py` now exposes `FastOptimalOracle` backed by resident native h48h7 plus `PortfolioOptimalOracle`, `RaceOptimalOracle`, `ResidentRaceOptimalOracle`, and `UniversalOptimalOracle`, which is the current top-level optimized exact path, `results/processed/fast_optimal_oracle_api_seed_2026_thesis_h48h7_trusted.json` directly exercises the FastOptimalOracle API with 4/4 exact verified rows and max runtime 2.664585s, `results/processed/universal_optimal_oracle_seed_2026_thesis_h48h7_lowload.json` proves the universal path exact/verified across solved fast path, superflip exact-certificate cache, and shallow resident race with deferred H48 startup, `results/processed/h48_resident_certification_seed_2026_thesis_h48h7_trusted.json` proves the resident hard certification corpus exact with `deterministic_depth_25` exact at 18 in 72.071228s and superflip exact at 20 in 77.822389s, `results/processed/nissy_public_table_install_seed_2026_thesis_pt_nxopt31_HTM_installed.json` installs the public Nissy `pt_nxopt31_HTM` table with CRC validation, `results/processed/optimal_3x3_seed_2026_stress_nissy_optimal.json` proves the external Nissy optimal stress corpus exact/verified with `random_3_20` at distance 17 in 23.734412s on 2 threads, `results/processed/optimal_3x3_seed_2026_thesis_nissy_core_direct_lowload.json` proves the local nissy-core shell can solve direct cube states with `solve -O 0 -cube`, `results/processed/race_optimal_oracle_seed_2026_thesis_h48h7_nissy_core_direct_lowload.json` proves the one-shot race can select direct `nissy-core` before representative-scramble recovery, `results/processed/portfolio_optimal_oracle_seed_2026_thesis_nissy_core_direct_state_lowload.json` proves the portfolio now selects that direct state path before representative-scramble recovery for source-less states, `results/processed/resident_race_optimal_oracle_seed_2026_thesis_h48h7_nissy_core_direct_lowload.json` proves the resident race can select direct `nissy-core` before representative-scramble recovery while keeping resident H48 deferred, the portfolio/race/resident-race artifacts prove their bounded exact control paths, and `results/processed/h48_oracle_contract_seed_2026_thesis_h48h7.json` machine-checks the source/API/table/evidence boundary and records `fast_optimal_oracle_implemented_for_every_valid_3x3_state: true`, `all_state_exact_contract_supported: true`, `empirical_fast_corpus_supported: true`, `race_nissy_core_direct_lowload_exact: true`, `portfolio_nissy_state_recovery_all_exact: true`, `portfolio_nissy_core_direct_state_all_exact: true`, `resident_race_nissy_core_direct_lowload_exact: true`, `race_optimal_oracle_lowload_exact: true`, `resident_race_optimal_oracle_lowload_exact: true`, `universal_optimal_oracle_lowload_exact: true`, and `fast_runtime_proven_for_every_possible_state: false`. The public Nissy 2.0.8 CLI remains scramble-oriented, while the local nissy-core shell path now supplies direct cube-state input for the H48-backed optimal backend. The package API now supports strongest-local H48 auto-selection for future h48h8-h48h11 tables, and `results/processed/h48_capacity_seed_2026_thesis_lowload.json` records h48h7 as the strongest local trusted table, h48h8 as the next missing table, no public H48 h48hN entries in the refreshed Nissy archive, h48h8 at 7,585,624,040 estimated bytes, h48h11 at 60,670,567,784 estimated bytes, and current 16 GB Mac h48h8 generation as unsafe. The trusted/preloaded superflip certification takes 158.440286s, the external Nissy optimal superflip probes timed out at 120s on 2 threads and 240s with 8 threads, the experimental auto-min-depth hard run timed out at 240s, an `H48_STARTING_MOVES=3` diagnostic was slower on the deterministic hard row, and h48h8 was not produced locally. Is this acceptable as public-solver-derived oracle-grade exact evidence, and what wording boundary is required before saying anything like "fast optimal oracle for arbitrary 3x3 states"?

11. Should the thesis compare against an external Kociemba package as a baseline after the native implementation is built?
12. What benchmark depth and number of cases are expected for an undergraduate/diploma evaluation?
13. Is the fixed-reference normalized Pocket Cube case study (`7! * 3^6` states, HTM `U/R/F` generators, DBL corner fixed) acceptable as the complete 2x2x2 exact-search component, provided the thesis clearly states the projection from labelled cube orientations?
14. The package now includes `RaceOptimalOracle`, which starts native H48 and external Nissy optimal subprocesses concurrently, `ResidentRaceOptimalOracle`, which races external Nissy against a resident `FastOptimalOracle` H48 session so repeated H48 calls avoid one-shot process/table setup, and `UniversalOptimalOracle`, which tries solved/certificate-cache/upper-lower proof paths before the resident race. The resident race can also delay H48 startup to reduce load on a busy Mac, and the saved universal artifact now proves the top-level path exact/verified on solved, cached superflip, and shallow live-race cases. These paths return exact only after independent verification and stop or avoid losing backends where possible; the contract/audit still keep `fast_runtime_proven_for_every_possible_state: false`. Is this acceptable wording as a "fast exact oracle engineering layer" while still avoiding a blanket every-state runtime guarantee?
15. The newest universal-oracle artifact `results/processed/universal_optimal_oracle_seed_2026_thesis_h48h7_nissy_core_direct_lowload.json` withholds the known source sequence and proves a raw `CubeState` reaches top-level `UniversalOptimalOracle`, then nested `ResidentRaceOptimalOracle`, then direct `nissy-core-direct` with `input_mode=cube_state`, `table_symlink=true`, `source_sequence_provided=false`, and exact verified distance 3 in 5.189862s on the loaded Mac. Is this enough to describe the universal API as accepting arbitrary valid cube states directly, while still leaving every-state runtime proof as unproven?
16. The newest universal batch artifact `results/processed/universal_batch_oracle_corpus_seed_2026_thesis_h48h7_resident_h48_batch_lowload.json` withholds all source sequences and proves 3/3 raw `CubeState` rows at deterministic depths 5, 10, and 15 through top-level `selected_backend=resident-h48-batch`, nested backend `resident-h48-batch`, `resident_native_h48=true`, `table_loaded_once=true`, and max wall runtime 18.413704s under one-thread low-priority execution on the loaded Mac. Is this acceptable wording as an "optimized resident native H48 batch exact-oracle layer for arbitrary valid 3x3 state input", provided the thesis still does not claim a practical runtime theorem for every possible 3x3 state?
17. The public CLI artifact `results/processed/universal_oracle_cli_seed_2026_thesis_h48h7_optimized_lowload.json` proves `rubik-optimal oracle --universal` on 3/3 raw facelet inputs with selected backends `upper-lower-certificate` for two rows and `resident-h48-batch` for one row, max row runtime 3.712856s, and `all_state_input_only=true`. Is this acceptable as the supervisor-facing wording for the user-facing exact oracle command, while keeping the formal caveat that this is corpus evidence rather than an all-state runtime theorem?
18. The broader public CLI artifact `results/processed/universal_oracle_cli_seed_2026_thesis_h48h7_broader_lowload.json` proves `rubik-optimal oracle --universal` on 5/5 raw facelet inputs at depths 5, 10, 15, 20, and 25, with two `resident-h48-batch` rows and one `portfolio-after-resident-h48-fallback` row after a bounded resident-H48 prepass, max row runtime 48.364612s, wrapper wall runtime 48.796864s, and `all_state_input_only=true` under one-thread low-priority execution. Is this broader corpus enough to describe the public command as an optimized exact-oracle interface with row-level timeout fallback, while still avoiding a blanket all-state practical-runtime claim?
19. The adaptive public CLI artifact `results/processed/universal_oracle_cli_seed_2026_thesis_h48h7_adaptive_lowload.json` proves the same 5/5 raw facelet corpus exact/verified after moving the portfolio/direct-state prepass before resident H48, with three `portfolio-before-resident-h48-batch` rows, max row runtime 10.477171s, and wrapper wall runtime 10.916268s under one-thread low-priority execution. Is this acceptable wording as the default optimized exact-oracle strategy for public state input, while still saying that a formal every-state practical-runtime proof remains unproven?
20. The expanded public CLI artifact `results/processed/universal_oracle_cli_seed_2026_thesis_h48h7_expanded_adaptive_lowload.json` proves 12/12 raw facelet rows exact/verified under the same public `rubik-optimal oracle --universal` command, including two deterministic rows at each depth 5, 10, 15, 20, and 25 plus named hard states `cli_hard_deterministic_depth_25` and `cli_hard_superflip_distance_20`; the superflip row is verified at distance 20 via `exact-certificate-cache`, all rows are state-only, max row runtime is 22.674068s, and the rows now include `state`/`solution` fields for certificate reuse. The expanded default certificate-cache artifact proves 1334/1334 symmetry-derived rows exact through zero-search cache, and the regenerated contract records `universal_oracle_cli_expanded_adaptive_lowload_exact=true` plus `certificate_cache_expanded_symmetry_closure_lowload_exact=true` while preserving `fast_runtime_proven_for_every_possible_state=false`. Is this enough public-interface/cache evidence for a thesis claim such as "optimized exact oracle interface for arbitrary valid 3x3 state input on the recorded corpus", and what additional proof or corpus would be required before using stronger "fast for every possible 3x3 state" wording?
21. The learned-certificate artifact `results/processed/learned_certificate_cache_seed_2026_thesis_h48h7_lowload.json` proves that newly solved exact/verified state-only rows can be appended to a JSONL certificate log and later replayed by a fresh `UniversalOptimalOracle` with live H48/Nissy backends disabled; replay selected `exact-certificate-cache` for 2/2 rows with max replay runtime 0.007890s. Is this acceptable wording as "persistent exact-certificate learning for repeated arbitrary-state queries", provided the thesis clearly states that unseen states still require live exact solving and that this is not an all-state runtime proof?
22. The resident-H48 symmetry artifact `results/processed/universal_optimal_oracle_seed_2026_thesis_h48h7_h48_symmetry_lowload.json` proves that the universal oracle can try a raw `CubeState` under deterministic axis-covering non-identity whole-cube rotations through resident native H48, map the exact rotated solution back, and verify it on the original cube before returning `exact`; the saved row selects `resident-h48-symmetry-batch`, configures two axis-covering variants, uses rotation `rot02`, withholds the source sequence, and solves the shallow case at distance 3 in 3.681836s under one-thread low-priority execution. Is this acceptable wording as "bounded direct-state H48 rotational-symmetry prepass to reduce orientation-sensitive live-search variance", while still saying that a formal fast runtime proof for every possible unseen state remains unproven?
23. The public CLI artifact `results/processed/universal_oracle_cli_seed_2026_thesis_h48h7_h48_symmetry_lowload.json` proves the same resident-H48 symmetry prepass through `rubik-optimal oracle --universal` and facelet input, after wiring the optional prepass into `UniversalOptimalOracle.solve_many`; the saved row selects `resident-h48-symmetry-batch`, configures two axis-covering variants, uses rotation `rot02`, withholds the source sequence, and solves a fresh deterministic depth-11 row at exact verified length 11 in 4.250191s under one-thread low-priority execution. Is this acceptable as public-interface evidence for the bounded H48 rotational-symmetry prepass, while still avoiding "fast for every possible 3x3 state" wording without a formal all-state runtime proof?
24. The public CLI artifact `results/processed/universal_oracle_cli_seed_2026_thesis_h48h7_live_no_shortcuts_lowload.json` proves `rubik-optimal oracle --universal` can disable both the exact certificate cache and upper/lower certificate shortcut for live-runtime evidence; the saved fresh depth-12 facelet row selects `resident-h48-symmetry-batch`, withholds the source sequence, returns exact verified length 12 in 4.615232s, and the regenerated contract records `universal_oracle_cli_live_no_shortcuts_lowload_exact=true` while preserving `fast_runtime_proven_for_every_possible_state=false`. Is this sufficient wording as "public live optimized exact-oracle evidence without cache shortcuts", and what broader live corpus or formal proof would be required before claiming practical fast runtime for every possible 3x3 state?
25. The broader public no-shortcut artifact `results/processed/universal_oracle_cli_seed_2026_thesis_h48h7_live_no_shortcuts_broader_lowload.json` proves 5/5 fresh facelet rows at depths 5, 10, 15, 20, and 25 exact/verified with both certificate-cache and upper/lower shortcuts disabled through a preloaded resident-H48 batch; selected backend is `resident-h48-batch` for all rows, max row runtime is 15.582677s, wrapper wall runtime is 15.736514s, and the regenerated contract records `universal_oracle_cli_live_no_shortcuts_broader_lowload_exact=true` while preserving `fast_runtime_proven_for_every_possible_state=false`. Is this depth-stratified live corpus sufficient for "fast live exact public-oracle evidence on the recorded corpus", and what proof method would be acceptable before saying "fast optimal oracle for every possible 3x3 state"?
26. The public streaming artifact `results/processed/universal_oracle_stream_seed_2026_thesis_h48h7_live_no_shortcuts_lowload.json` proves `rubik-optimal oracle --stream --universal` keeps one universal oracle alive and solves 3/3 line-delimited rows exact/verified with certificate-cache and upper/lower shortcuts disabled; the two non-solved rows reach live `resident-race`, max row runtime is 4.049983s, and wrapper wall runtime is 8.005637s under one-thread low-priority execution. Is this acceptable wording as an interactive "resident universal optimal oracle" interface, while still avoiding the stronger claim that every possible 3x3 state has a proven practical runtime bound?
27. The known-distance benchmark artifacts now prove public `rubik-optimal oracle --universal` on facelet states from nissy-core's benchmark corpus with source sequences withheld and certificate/upper-lower shortcuts disabled: the forced-H48 artifact proves distance 17 through `resident-h48-batch` with backend solve time 32.012803s; the adaptive artifact proves distances 17 and 18 through `portfolio-before-resident-h48-batch` with exact Nissy optimal batch prepass, max row runtime 54.702123s; the distance-19 adaptive artifact proves exact verified length 19 after Nissy uses its 180s prepass budget and resident native H48 completes the exact proof with backend solve time 39.361181s, max row runtime 227.559559s; and the two distance-20 adaptive artifacts prove exact verified length 20 after Nissy uses its 300s prepass budget and resident native H48 completes each exact proof: row 0 has backend solve time 200.768737s and max row runtime 508.871251s, while row 1 has backend solve time 174.484259s and max row runtime 482.494329s, with `outer_command_timed_out=false` for both rows. A bounded-prepass artifact then proves row 1 exact/verified again after capping the Nissy prepass at 30s and disabling H48 fallback inside the prepass; selected backend is `resident-h48-batch-after-portfolio-prepass`, backend solve time is 120.182279s, max row runtime is 158.304472s, and wrapper wall runtime is 158.514775s. Contract/audit checks pass while preserving `fast_runtime_proven_for_every_possible_state=false`; the remaining 23 public distance-20 benchmark rows are not claimed yet. Is this acceptable as public known-distance hard-corpus and hard-row speed evidence through God's Number, and what larger known-distance/random/worst-case corpus or proof boundary is required before using "fast for every possible 3x3 state" wording?
28. The repository now includes `scripts/run_known_distance20_trimmed_prepass_sweep.py`, a low-load resumable runner that advances the remaining public distance-20 benchmark rows one offset at a time through the optimized public universal CLI, with 30s Nissy prepass, trusted/preloaded h48h7, shortcuts disabled, one-thread defaults, `nice -n 20`, and skip/resume behavior. Is a staged campaign over these 25 public distance-20 rows acceptable as the next empirical boundary for the thesis, or should the remaining effort go toward a formal H48 runtime argument or stronger-table generation on a larger machine instead?
29. The first staged offset-2 run timed out through the current optimized h48h7 path: Nissy timed out at 30s, resident h48h7 timed out at 420s, fallback Nissy timed out at 30s, and a bounded six-rotation H48 rescue probe also failed to return before its 600s wrapper timeout. Given this hard-tail evidence, should the thesis explicitly stop at "implemented exact oracle with empirical fast corpus but no every-state runtime guarantee", or is the expected scope to secure larger hardware/stronger H48 tables before submission?
30. H48 table generation now uses a staged `.partial` output and atomic publish after successful native generation plus expected-size validation, and the current 16 GiB Mac still refuses h48h8 generation under `--require-safe` because h48h8 is about 7.06 GiB before generator overhead. Is this safe-generation boundary acceptable for the thesis implementation section, or should a larger-memory machine be used to attempt h48h8 before final review?
31. The new parallel-H48 symmetry race solves deterministic whole-cube rotations concurrently, verifies the mapped-back exact solution, and terminates losing native processes. A public no-shortcut CLI artifact proves one fresh depth-11 row exact/verified through `selected_backend=parallel-h48-symmetry-race` in 1.456214s, but the distance-20 offset-2 hard-tail timeout has not yet been rerun with this path. Should the next evidence step be a bounded offset-2 rerun with parallel H48 symmetry, or should the thesis treat this as an engineering optimization and keep the larger h48h8/stronger-heuristic work as future work?
32. The bounded offset-2 rerun with parallel H48 symmetry has now been run. The hard-row sweep wrapper exposes `--h48-parallel-symmetry-variants`, `--h48-parallel-symmetry-timeout`, and `--no-preload-table`, and the universal oracle now accounts for failed H48 prepass time in later fallback rows. The corrected public artifact `results/processed/universal_oracle_cli_seed_2026_thesis_h48h7_known_distance_20_offset2_trimmed_prepass_h48_180_parallel_h48sym2_180_nopreload_lowload.json` still times out: the parallel H48 prepass spends 180.186780s, resident h48h7 spends 180.004593s, fallback Nissy spends 30.081626s, row runtime is 422.411451s, wrapper wall is 422.572496s, and `passed=false`. Is this enough evidence to stop claiming a local every-state fast oracle on h48h7 and move the remaining ambition to stronger tables/heuristics/larger hardware, or does the thesis require a new implementation track such as h48h8+ or additive/cost-partitioned PDBs before review?
33. The universal oracle now also separates the cheap Nissy/portfolio prepass budget from the late Nissy fallback budget after resident H48 misses. The public CLI exposes `--universal-portfolio-fallback-timeout`, and the distance-20 sweep wrapper exposes `--fallback-timeout`. A load-aware offset-2 probe used one thread, `nice -n 20`, no preload, 30s Nissy prepass, 10s resident h48h7, and 300s late Nissy fallback; the saved artifact `results/processed/universal_oracle_cli_seed_2026_thesis_h48h7_known_distance_20_offset2_trimmed_prepass_h48_10_nissyfb300_nopreload_nissyfallback_lowload.json` still timed out with late Nissy runtime 300.083586s, row runtime 341.990964s, wrapper wall 342.162913s, and `outer_command_timed_out=false`. Does this close the "maybe the fallback just needed a real timeout" question enough to document h48h7/Nissy hard-tail limits, or should the thesis continue into stronger tables/hardware/heuristic design before supervisor review?
34. The native H48 backend now supports `--lower-bound-batch`, and the universal oracle can compute the maximum admissible H48 lower bound over all 24 whole-cube rotations before accepting an upper/lower certificate. The direct public artifact `results/processed/universal_oracle_cli_seed_2026_thesis_h48h7_rotational_lower_bound_certificate_direct_lowload.json` disables the exact-certificate cache and proves a source-less facelet row exact through `upper-lower-certificate` with `h48_lower_bound=3`, `rotation_count=24`, and row runtime 0.01803450006991625s. Is this acceptable wording as "rotational H48 lower-bound certification can avoid full IDA* for states whose verified upper bound matches the strongest admissible rotated lower bound", while still saying that hard-tail states without matching bounds require live search and do not have a proven every-state fast-runtime guarantee?
35. The public universal oracle now tries direct `nissy-core` cubie-state solving as a late fallback after resident H48 batch timeout, with a separate `--universal-fallback-nissy-core-direct-timeout`. The artifact `results/processed/universal_oracle_cli_seed_2026_thesis_h48h7_late_nissy_core_direct_fallback_lowload.json` disables cache/upper-lower shortcuts, forces resident H48 to timeout, withholds the source sequence, and solves a direct facelet row exactly through nested `selected_backend=nissy-core-direct` in 2.500632s backend time and 4.573622s row time. Is this acceptable wording as "late optimized direct-state exact fallback after resident-H48 timeout", while still saying that this does not prove fast runtime for every possible 3x3 state or solve the public distance-20 hard-tail offset-2 timeout?
36. The native Korf/IDA* Nissy-heuristic bridge now fixes direct cubie-state conversion for side-axis corner orientations and enables axis-transform DR/UD lower bounds for source-less facelet/CubeState inputs by default. A direct probe confirms state-array and sequence conversions now match for both a shallow state and the deterministic hard stress sequence; the hard stress lower bound improves from 9 to 10 under axis transforms, and a source-less facelet CLI smoke solves `R U F2` exactly with `nissy_axis_transforms=True`. Is this acceptable wording as "optimized admissible Nissy DR/UD heuristic support for direct arbitrary-state native IDA* input", while still saying it is not a complete cost-partitioned PDB system and does not prove every possible 3x3 state is fast?
37. The native Korf/IDA* solver now supports configurable child ordering and defaults to `heuristic-desc` through the C++ binary, Python wrapper, public CLI, and benchmark script. Focused low-load tests pass, and a source-less `R U F2` native/Nissy smoke records `child_order=heuristic-desc` with exact length 3, but the change is only search ordering after admissible pruning. Is this acceptable wording as "optimized native exact-search ordering", while still presenting additive/cost-partitioned PDBs, stronger H48 tables, or formal runtime analysis as the remaining route to a stronger every-state speed claim?
38. The public H48 rotational symmetry race now supports a maximum active-process cap through `--h48-parallel-symmetry-max-concurrency`, scales its outer deadline by the number of rotation waves, and the distance-20 sweep runner can plan all 24 whole-cube rotations for the offset-2 hard row while running only two native H48 processes at once on the loaded Mac. Is this the right next empirical campaign toward the fast-all-state claim, or should the thesis instead stop the local evidence at h48h7 hard-tail limits and move the stronger claim to future h48h8+/larger-hardware work?
39. The H48-backed oracle paths now expose native `--auto-min-depth` as an explicit exact-safe option, so selected experiments can start at the admissible H48 lower bound rather than at depth zero. The option remains off by default because prior hard-tail evidence showed it can be counterproductive on difficult rows. Is it acceptable to present this as an available admissible-start optimization, while still saying that a full "fast for every possible 3x3 state" claim requires stronger hard-tail evidence or a formal runtime boundary?
40. The public universal oracle and distance-20 sweep now expose exact-safe Nissy whole-cube rotational batching through `--nissy-symmetry-variants` and `--nissy-symmetry-timeout`, and a dry-run plans public distance-20 offset 2 with 23 Nissy rotations plus a bounded H48 rotational race without launching the heavy solver on the loaded Mac. Should this be run as the next long empirical campaign, or should the thesis instead use the existing offset-2 timeout evidence as the local h48h7 boundary?
41. The public universal oracle and distance-20 sweep now also expose exact-safe direct `nissy-core` cubie-state rotational racing through `--nissy-core-direct-symmetry-variants`, `--nissy-core-direct-symmetry-timeout`, and `--nissy-core-direct-symmetry-max-concurrency`. A dry-run for public distance-20 offset 2 plans 23 direct-core rotations capped at two concurrent native processes, 23 Nissy whole-cube rotations, and two parallel H48 rotations under one-thread `nice -n 20` execution, with adaptive `command_timeout_seconds=3270.0`, but no heavy solver was launched on the loaded Mac. Should this combined direct-core/Nissy/H48 symmetry campaign be run as the next empirical attempt, or should the thesis document the h48h7 hard-tail limit and move stronger all-state speed claims to h48h8+/larger hardware/future work?
42. The combined direct-core/Nissy/H48 symmetry campaign from question 41 has now been run on public distance-20 offset 2 under low machine pressure. It still timed out after 3226.388648s: all 24 direct `nissy-core` rotations timed out, Nissy symmetry timed out, parallel H48 timed out, resident H48 timed out, and the final 300s Nissy fallback timed out. The run also exposed and triggered a fix for compiled H48 parallel-race per-candidate wall-clock timeout enforcement. Is this enough evidence to keep the thesis claim at "implemented exact oracle with strong empirical corpus and documented h48h7 hard-tail limits", or is the expected thesis target a larger-machine/stronger-table campaign such as h48h8+ before final review?
43. The repository now imports the vendored nissy-core known-distance benchmark scrambles as exact replay certificates after verifying inverse solutions locally. The generated artifact covers 125 rows for distances 16-20, and the formerly timed-out distance-20 offset-2 state now returns exact/verified through `exact-certificate-cache` in 3.052423s row runtime. Is it acceptable to report this as "fast exact replay for public known-distance benchmark states" while clearly separating it from live-search performance on unseen arbitrary states?
44. The full imported public known-distance benchmark set has now been replayed through the public `rubik-optimal oracle --universal` CLI with source sequences withheld and expensive live fallback disabled: `results/processed/universal_oracle_cli_seed_2026_thesis_h48h7_known_distance_16_20_certificate_cache_lowload.json` records 125/125 distance-16 through distance-20 rows exact/verified via `exact-certificate-cache`, all expected distances matched, max row runtime 1.690597s, and wrapper wall runtime 1.926562s on a loaded 8-logical-CPU, 16 GiB Mac. Is this sufficient as thesis evidence for "optimized exact certificate replay over the public known-distance benchmark corpus", while keeping the stronger "fast for every possible unseen 3x3 state" claim out of scope unless a stronger table/formal proof/larger-machine campaign is approved?
45. The stronger-table campaign runner now makes the h48h8 path executable but refuses unsafe local generation on the currently loaded 8-logical-CPU, 16 GiB Mac. `results/processed/h48_capacity_seed_2026_thesis_current_20260531_goal.json` records h48h7 as the strongest trusted local table, h48h8 as the next missing table, and `can_claim_fast_oracle_for_every_possible_state=false`; `results/processed/h48_stronger_table_campaign_seed_2026_thesis_h48h8_current_20260531_goal.json` records `status=refused_unsafe_generation`, `target_estimated_table_size_bytes=7585624040`, `should_run_generation=false`, and the exact low-priority one-thread h48h8 generation command to run when a safe machine is available. Should the thesis require running this h48h8 campaign on a larger/idle machine before review, or should the final claim boundary remain "optimized exact oracle and fast certificate/corpus evidence, but no proven practical runtime for every unseen 3x3 state"?
46. The repository now has a controlled external RubikOptimal backend and safe table-generation script. The bridge checks the full expected 963,061,264-byte table inventory before solving, runs the package out-of-process through PyPy, and verifies returned solutions locally. The first low-priority table build was stopped after 192.944366s when machine load spiked, but the later guarded one-worker run completed the remaining `phase1x24_prun` table: `results/processed/rubikoptimal_tables_seed_2026_thesis_current_20260531_phase1x24.json` records `ready=true`, `passed=true`, `runtime_seconds=1067.615984`, and no missing or wrong tables. The new `rubik-optimal oracle --rubikoptimal` path and `scripts/run_rubikoptimal_oracle_corpus.py` produced a 6-row arbitrary-state corpus with source sequences withheld from the solver: `results/processed/rubikoptimal_oracle_corpus_seed_2026_thesis_lowload.json` records `passed=true`, `all_exact=true`, `all_verified=true`, `all_rubikoptimal_batch=true`, `batch_wall_seconds=86.174305`, and max row runtime 79.214607s, including a deterministic source-depth-20 state solved at exact length 17. The non-stream oracle uses the resident batch helper, so the 963 MB table set is loaded once per batch instead of once per row; the real two-row CLI smoke records shared `table_load_seconds=8.562476083170623`. RubikOptimal is now also wired into `UniversalOptimalOracle` behind `--universal-rubikoptimal-prepass-timeout` and `--universal-rubikoptimal-fallback-timeout`; a real two-row low-priority prepass smoke selected `rubikoptimal-prepass`, loaded the 963 MB table set once, and solved both rows exact/verified in 2.6386912912130356s wall time. A separate canonical superflip row in `results/processed/rubikoptimal_oracle_corpus_seed_2026_thesis_superflip_lowload.json` timed out at 300.030517s. Should the thesis report RubikOptimal as a table-complete exact public backend with resident-batch, universal-prepass, and universal-fallback optimization, nontrivial corpus evidence, and documented hard-state timeout, while keeping "fast for every possible state" out of scope unless a stronger/larger-machine RubikOptimal campaign clears superflip and broader distance-20 rows?
47. The public known-distance certificate replay now covers all 125 imported nissy-core benchmark rows for distances 16 through 20 through the public `rubik-optimal oracle --universal` CLI with source sequences withheld: `results/processed/universal_oracle_cli_seed_2026_thesis_h48h7_known_distance_16_20_certificate_cache_lowload.json` records `passed=true`, all exact/verified, all expected distances matched, selected backend `exact-certificate-cache`, max row runtime 1.690597s, and wrapper wall runtime 1.926562s. However, the follow-up live RubikOptimal-enabled distance-20 offset-2 run still timed out under low-priority one-thread execution: `results/processed/universal_oracle_cli_seed_2026_thesis_h48h7_known_distance_20_offset2_trimmed_prepass_h48_180_ropre300_rorace120_rofb300_nopreload_rubikoptimal_live_lowload.json` records `passed=false`, row status `timeout`, selected backend `rubikoptimal-after-universal-fallback`, max runtime 630.127028s, and a final 300.018660s RubikOptimal fallback timeout. Should the thesis present the 125-row certificate replay as fast exact replay evidence while treating the live offset-2 timeout as the hard boundary for unseen-state performance on this Mac, or should a larger-machine/stronger-table campaign be required before review?
48. The universal oracle now also exposes a RubikOptimal whole-cube rotational batch path: the solver can rotate an input state through up to 23 non-identity cube rotations, call RubikOptimal in one shared batch, map any exact rotated solution back, and verify it on the original cube before returning `exact`. The public CLI and distance-20 sweep expose `--universal-rubikoptimal-symmetry-variants` and `--universal-rubikoptimal-symmetry-timeout`; focused tests and contract generation pass, and a low-load dry-run planned public distance-20 offset 2 with 23 rotations, 300s per rotation, one solver thread, no H48 preload, `nice -n 20`, and adaptive wrapper timeout 8070s. Given the Mac is under heavy general use, should this 8070s campaign be run locally, moved to a larger/idle machine, or left as future work while reporting the existing live offset-2 RubikOptimal timeout as the hard boundary?
49. The RubikOptimal whole-cube path now also has a bounded concurrent race mode. With `--rubikoptimal-symmetry-max-concurrency 2`, the public distance-20 offset-2 dry-run still tests 23 rotations at 300s each, but only two RubikOptimal processes run at once and the adaptive wrapper timeout drops from 8070.0s to 4770.0s. Focused and impacted tests pass, and the contract/verification/audit remain clean, but no live 4770s hard-tail campaign was launched on the busy Mac. Should this lower-pressure race campaign be run locally, scheduled only on an idle/larger machine, or kept as future work while the thesis states the exact-safe optimization is implemented but every-state fast runtime remains unproven?
50. The resident race now starts direct `nissy-core` and `nissy-symmetry-batch` as concurrent Nissy-side exact candidates when both are explicitly configured for source-less state input, so the direct-core candidate no longer shadows the rotational Nissy batch. Focused tests prove a symmetry winner stops the direct-core loser, and the public evidence wrapper now accepts `rubikoptimal-symmetry-race` as a valid selected backend. Is this exact-safe orchestration improvement enough to justify another low-load hard-tail campaign, or should the remaining every-state speed claim still be deferred to stronger tables/larger hardware/formal proof?
51. The universal oracle now exposes an opt-in resident-race prepass through `resident_race_prepass_timeout_seconds` and public `--universal-resident-race-prepass-timeout`, so a bounded concurrent H48/Nissy/RubikOptimal race can run before later sequential hard-tail phases. Focused tests prove exact prepass wins return as `selected_backend=resident-race-prepass`, timeout prepass results are preserved in later notes, and the public wrapper budgets/counts this phase. Is this the right low-load default campaign mechanism for the next hard-tail attempt, or should this remain opt-in while the thesis keeps the fast-every-state runtime claim deferred to stronger tables/larger hardware/formal proof?
52. The known-distance distance-20 sweep runner now forwards that resident-race prepass through `--resident-race-prepass-timeout`, records `_rrpre...` in artifact suffixes, includes it in adaptive command-timeout estimates and resume checks, and a dry-run for offset 2 planned one-thread/no-preload execution with 120s resident race, 120s RubikOptimal prepass/race, 300s RubikOptimal fallback, and a 1230s wrapper budget. Given the Mac is under general load, should this 20-minute single-row campaign be run locally, moved to an idle/larger machine, or kept as future work while the thesis states that fast runtime for every unseen state remains unproven?
53. That campaign has now been run locally under one-thread `nice -n 20` execution. The saved aggregate artifact `results/processed/known_distance_sweep_seed_2026_thesis_h48h7_known_distance_20_trimmed_prepass_h48_180_nissyfb300_rrpre120_ropre120_rorace120_rofb300_nopreload_sweep_rrpre_ro_lowload.json` records `passed=false`, row `status=timeout`, `runner_wall_seconds=573.011655`, and `selected_backend=rubikoptimal-after-universal-fallback`; the single-row notes show a 30s Nissy portfolio timeout, 120s resident-race prepass timeout, 120s RubikOptimal prepass timeout, and final 300.026674s RubikOptimal fallback timeout. Is this sufficient evidence to present the local all-state-speed target as not achieved on this Mac, or should a larger/idle-machine campaign with stronger tables or broader RubikOptimal symmetry racing be required before thesis review?
54. The same public distance-20 offset-2 row has now also been isolated to resident native h48h7 with no portfolio prepass, no certificate cache, no upper/lower shortcut, no direct Nissy fallback, no RubikOptimal phases, no H48 preload, one thread, and `--h48-auto-min-depth`. The artifact `results/processed/universal_oracle_cli_seed_2026_thesis_h48h7_known_distance_20_offset2_h48_automin420_nopreload_lowload.json` still records `passed=false`, row `status=timeout`, `runtime_seconds=423.194403`, and resident H48 timeout after 420.008831s. Does this make the next required technical step a stronger H48 table / larger hardware / multi-threaded idle-machine campaign, rather than more local one-thread h48h7 orchestration changes?
55. The hard-tail sweep now has an explicit idle guard for the larger RubikOptimal/H48/Nissy campaigns. With `--wait-for-idle --max-load1 2.5 --max-load5 3.0 --min-available-gib 6`, the planned public distance-20 offset-2 RubikOptimal symmetry-race campaign was deferred rather than launched on the currently loaded 8-logical-CPU, 16 GiB Mac; `results/processed/known_distance_sweep_seed_2026_thesis_h48h7_known_distance_20_trimmed_prepass_h48_180_nissyfb300_ropre120_rosym23_300_conc2_rofb300_nopreload_sweep_ro_sym_idle_guard_currentload.json` records `attempted_new_runs=0`, `idle_deferred_offset_count=1`, load about 4.24/5.27, and conservative available memory about 0.16 GiB. Should the thesis require this guarded campaign to be rerun on an idle/larger machine before review, or is the honest boundary now "optimized exact oracle implemented, fast replay/corpus evidence proven, every-state live runtime still unproven"?
56. The native H48 rotational race now has an exact-safe lower-bound ordering mode: before launching expensive exact H48 searches, it can run a cheap lower-bound batch over the whole-cube rotations and try the rotations with stronger admissible lower bounds first. The public oracle and distance-20 sweep expose `--h48-parallel-symmetry-order-by-lower-bound`, but the current guarded offset-2 campaign was deferred by the idle guard because the Mac reported load about 6.71/11.53 and about 0.15 GiB conservative available memory. Should this lower-bound-ordered H48 campaign be run on an idle/larger machine, or should the thesis stop at "implemented exact-safe optimization, not yet hard-tail success evidence"?
57. The same H48 lower-bound rotation ordering is now shared by the Nissy symmetry batch, direct `nissy-core` rotational race, RubikOptimal symmetry batch, and RubikOptimal symmetry race. Focused tests pass, and the wrappers record `symmetry_order_by_h48_lower_bound`, but the current Mac was too busy to justify a live hard-tail campaign. Should this shared-ordering campaign be run only on an idle/larger machine, or should it be documented as implemented optimized scheduling while the thesis keeps the all-state practical-runtime claim unproved?
58. The public `rubik-optimal oracle --stream --rubikoptimal` path now reuses one resident RubikOptimal session instead of reloading the 963 MB table set per input line. Focused tests and the regenerated contract prove the public stream wiring, but no real table-backed stream artifact was launched while the Mac reported load around 11.76/12.67/10.99. Should a low-load RubikOptimal stream artifact be required before review, or is the resident API evidence plus stream wiring enough for the current thesis boundary?
59. The RubikOptimal whole-cube rotational race now uses a resident worker pool: with bounded concurrency, a fixed set of `RubikOptimalOracleSession` workers claims rotations sequentially instead of constructing a separate resident session job for every rotation. Focused tests prove one resident worker can process multiple rotations and still return only a mapped-back, independently verified exact solution; the regenerated contract records `rubikoptimal_rotational_race_uses_resident_worker_pool=true`. The resident child now also enforces solve timeouts internally with `signal.setitimer`, returns a structured timeout row, and remains alive for the next query; the focused timeout-survival test proves `resident_start_count=1` across a timed-out query followed by an exact query, and the contract records `rubikoptimal_resident_timeout_keeps_loaded_process=true`. No live hard-tail campaign was run while the Mac was loaded. Is this enough to document the RubikOptimal symmetry-race implementation as optimized, or should a real idle-machine table-backed symmetry-race artifact be required before review?
60. The sequential RubikOptimal batch helper now also uses one resident `RubikOptimalOracleSession` instead of a one-shot subprocess. Focused tests prove a batch can return `exact`, `timeout`, `exact` while keeping `resident_start_count=1`, and the regenerated contract records `rubikoptimal_batch_uses_resident_session=true` while preserving `fast_runtime_proven_for_every_possible_state=false`. Given the current Mac load, should this optimized resident-batch implementation plus focused tests be enough for thesis implementation evidence, or should a real idle-machine RubikOptimal batch/symmetry hard-tail artifact be required before final review?
61. The external Nissy batch bridge now preserves completed exact rows when a later batch row times out: partial solution output is parsed and independently verified, completed rows record `partial_timeout_recovered=true`, and unfinished rows record `partial_completed_count`. The regenerated contract records `external_nissy_batch_recovers_partial_timeout_rows=true`. Is this exact-safe timeout recovery sufficient for the Nissy batch implementation claim, or should a real hard-tail Nissy batch artifact with partial timeout recovery be produced on an idle/larger machine?
62. The external Nissy batch bridge now also sorts pending representative scrambles by length before invoking `solve ... -i`, while restoring results to original row indices and verifying every recovered row. Focused tests prove a shorter original second row is sent first and recovered exactly before a longer hard row times out, and the contract records `external_nissy_batch_orders_shorter_scrambles_first=true`. Is this heuristic but exact-safe ordering acceptable implementation evidence, or should row ordering be driven by H48 lower bounds or a real hard-tail artifact before review?
63. The resident-race Nissy symmetry competitor now can order its identity-plus-rotation batch by H48 lower bound through `symmetry_order_by_h48_lower_bound`; the universal resident-race prepass forwards this shared setting, focused tests verify the propagation and H48-ordering call, and the contract records `resident_race_nissy_symmetry_orders_by_h48_lower_bound=true`. Is this enough exact-safe optimization evidence for the early concurrent hard-tail phase, or should the thesis require an idle/larger-machine distance-20 campaign using this ordered resident race before stronger wording is approved?
64. The public sweep and universal wrapper now expose the clearer shared alias `--symmetry-order-by-h48-lower-bound` independently of `--h48-parallel-symmetry-variants`; a dry-run proves the distance-20 resident-race/Nissy-symmetry campaign can request H48 lower-bound ordering without launching parallel H48 workers. Given the Mac remains under heavy general use, should this dry-run plus contract/test evidence be enough for the implementation claim, or should an idle/larger-machine hard-tail run be required before any stronger practical-runtime wording?
65. The portfolio/universal exact certificate path now can improve Kociemba upper bounds by trying whole-cube rotations, mapping a shorter rotated solution back, independently verifying it, and certifying exactness only when that upper bound equals the admissible H48 lower bound. The public CLI exposes `--kociemba-upper-bound-symmetry-variants`, focused/impacted tests pass, and the contract records `portfolio_oracle_can_use_kociemba_symmetry_upper_bound_certificate=true` plus `public_oracle_cli_exposes_kociemba_symmetry_upper_bound=true`, while `fast_runtime_proven_for_every_possible_state=false` remains. Is this sufficient optimized-oracle implementation evidence for the thesis boundary, or should a broader idle-machine corpus artifact be required before using any stronger practical-runtime wording?
66. The native H48 backend now separates completed negative bounded search from real timeout by returning `status=lower_bound` with `proved_lower_bound=max_depth+1`, and the portfolio/universal oracle can use this to certify a verified upper-bound solution of length `U` by proving no H48 solution exists below `U`. The public CLI exposes `--h48-upper-bound-proof-timeout` and `--h48-upper-bound-proof-max-gap`, focused/impacted tests pass, and the contract records `backend_reports_completed_negative_search_as_lower_bound=true`, `python_h48_wrapper_preserves_bounded_search_lower_bound=true`, `portfolio_oracle_can_use_h48_upper_bound_proof=true`, and `public_oracle_cli_exposes_h48_upper_bound_proof=true`, while `fast_runtime_proven_for_every_possible_state=false` remains. Is this bounded-proof exactness mechanism sufficient implementation evidence, or should the thesis require an idle/larger-machine hard-tail run showing this path closes a distance-19/20 public benchmark row before stronger runtime wording is approved?
67. The distance-20 sweep runner now forwards the bounded H48 upper-bound proof settings into the public universal CLI, records them in artifact suffixes and resume checks, and budgets them in adaptive command timeouts. A focused dry-run for public distance-20 offset 2 planned a one-thread `nice -n 20` command with `--h48-upper-bound-proof-timeout 120`, `--h48-upper-bound-proof-max-gap 4`, and `command_timeout_seconds=690.0`, but the same dry-run recorded the local Mac as non-idle with load about 4.68/5.02 and only about 0.16 GiB conservative available memory against a 6 GiB guard. Should this bounded-proof hard-tail campaign be moved to cloud or an idle larger machine before final review, and if so what runtime target, sample size, and acceptable cloud-cost boundary should be approved?
68. The repository now includes `scripts/plan_cloud_hardtail_campaign.py`, which generated `results/processed/cloud_hardtail_campaign_plan_seed_2026_thesis_h48h7_cloud_hardtail_20260531.json` with 30 off-Mac workloads: all 25 public distance-20 rows as one-row optimized universal-oracle shards, h48h8 stronger-table generation/certification, RubikOptimal superflip hard-case validation, and postprocess contract/audit commands. The plan recommends at least 16 CPU, 64 GiB RAM, and 250 GiB local NVMe, and keeps `fast_runtime_proven_for_every_possible_state=false` until the artifacts pass. Is this the right cloud campaign scope before thesis review, or should the scope be narrowed to a cheaper canary first, such as distance-20 offsets 2-4 plus RubikOptimal superflip?
69. The cloud campaign now has `scripts/run_cloud_hardtail_workload.py` for one-workload execution and `scripts/evaluate_cloud_hardtail_campaign.py` for aggregate proof evaluation. The current local evaluation correctly fails because only a dry-run workload exists and none of the 30 real cloud workloads has been executed. Should the supervisor accept this harness as the required reproducibility structure for an off-Mac proof campaign, and should the first cloud run execute the full 30-workload plan or only a smaller canary shard set before spending on the full run?
70. The H48 oracle contract now has an evidence-driven `cloud_runtime_proof` gate rather than a hardcoded false flag: a full-scope cloud evaluation can only flip `fast_runtime_proven_for_every_possible_state` after all required workloads pass, full distance-20 coverage is present, no workload remains missing/failed, and the thesis audit gates pass. A separate canary plan has also been generated for distance-20 offsets 2-4 plus h48h8/RubikOptimal hard targets, but `claim_scope=canary` prevents it from satisfying the final every-state runtime proof. Should the first off-Mac run use this canary to validate cost/runtime behavior, or should we run the full 30-workload plan immediately?
71. The cloud proof now has a campaign-level runner, `scripts/run_cloud_hardtail_campaign.py`, which can execute selected workloads, resume already-passed workload records, stop on failure, apply estimated timeouts, and write the aggregate evaluation automatically. A local canary dry-run selected all 8 canary workloads but executed none, and the contract still prefers the full 30-workload evaluation for the final gate. Should the first off-Mac run use this runner on the canary plan with `--resume --stop-on-fail`, then expand to the full 30-workload plan only if the canary runtime/cost profile is acceptable?
72. The cloud campaign now has generated shell runbooks under `results/cloud_hardtail_runbook_cloud_20260531/`: `run_canary.sh`, `run_full.sh`, `evaluate_full.sh`, and `collect_results.sh`, plus a README and machine-readable manifest. They default to 16 threads, resume passed workloads, stop on the first failed real workload, use estimated timeouts, and package the processed artifacts for review. Is this sufficient operational structure for starting with a paid cloud canary, or should the runbook include stricter spending/time caps before the first off-Mac execution?
73. The full campaign can now be split across machines. `results/cloud_hardtail_runbook_cloud_20260531_p10/` assigns the 25 distance-20 hard-tail shards across 10 worker scripts and keeps h48h8/RubikOptimal hardcase work in a separate non-shard script. The scaled estimate is about 8.125h with 10 shard workers plus one leader/non-shard machine, or roughly 9-10h if exactly 10 total machines are used with one reserved for non-shard work. Is this parallel cloud plan an acceptable cost/time tradeoff for trying to prove the fast-every-state runtime claim, or should the thesis stop at the current implementation/corpus evidence?
74. The cloud hard-tail planner now has `--hardtail-execution-mode batch`, and the generated full batch plan `results/processed/cloud_hardtail_campaign_plan_seed_2026_thesis_h48h7_cloud_hardtail_batch10_20260601.json` groups the 25 public distance-20 rows into three direct universal-oracle batch workloads instead of 25 one-row sweep-wrapper workloads. This records an 8.333333x reduction in hard-tail public CLI process launches and reuses resident H48/Nissy/RubikOptimal table-loaded sessions across rows, while the evaluator still requires every row to be exact, verified, expected-distance matched, and source-sequence-free. This is compute-efficiency work, not just parallelism, but it still does not prove a 10x reduction in worst-case search-node expansion. Should the off-Mac campaign use this batch10 plan first, or should the stronger h48h8 table campaign be required before trying to support any stronger "fast for every possible state" wording?
75. The newer h48h8-first cloud plan `results/processed/cloud_hardtail_campaign_plan_seed_2026_thesis_h48h8_cloud_h48h8_hardtail_batch10_20260601.json` makes h48h8 table generation/certification a prerequisite for the hard-tail batches and then runs all 25 public distance-20 rows with `solver=h48h8`, rather than generating h48h8 beside an h48h7 hard-tail run. The runbook `results/processed/cloud_hardtail_runbook_cloud_20260601_h48h8_batch10.json` adds explicit prerequisite/table-distribution scripts; after accounting for the prerequisite stage, its scaled 3-machine end-to-end estimate is about 22.292 hours. The current h48h8 contract remains false because no cloud workload has run. If the thesis needs stronger "fast for every possible state" wording, should this h48h8-first campaign be the next paid/idle-machine run, or is the h48h7 implementation/corpus evidence plus honest limitation already sufficient for review?
76. The capacity and cloud-plan logic now distinguishes h48h8 as the first missing stronger-table ladder step from h48h11 as the actual fast target selected from the vendored upstream 16-thread H48 benchmark hints. The new fast-target artifacts `results/processed/cloud_hardtail_campaign_plan_seed_2026_thesis_h48h11_cloud_h48h11_fasttarget_hardtail_batch10_20260601.json` and `results/processed/cloud_hardtail_runbook_cloud_20260601_h48h11_fasttarget_batch10.json` require H48H11 table generation/certification before hard-tail rows. After machine sizing was tied to the actual 60,670,567,784-byte H48H11 table, the runbook now recommends 16 CPU, 128 GiB RAM, and 500 GiB local NVMe, with a scaled 3-machine estimate of about 19.410 hours end-to-end or 4.410 hours after prerequisite tables are already generated and validated. The runbook now enforces leader preflight before real canary/full/prerequisite execution, and the current local H48H11 preflight artifact records `passed=false` because the laptop has 8 CPUs, 16 GiB RAM, and about 20.131 GiB free disk. The h48h11 contract remains false because no H48H11 table or runtime artifacts exist. If the thesis requires the phrase "fast optimal oracle for every possible 3x3 state", should the next paid/idle-machine run target H48H11 directly on this larger cloud shape, or should the thesis keep the current implementation/corpus evidence with the honest runtime limitation?
77. The proof gate now refuses to let h48h7 or h48h8 cloud artifacts satisfy the H48H11 fast-target claim: the evaluator checks solver/table/distance/batch-size evidence, and the contract requires `solver=h48h11` plus `target_table_trusted=true` before the every-state fast-runtime flag can become true. The regenerated H48H11 contract matches the target solver but still has `h48_fast_target_table_trusted=false` and all 8 fast-target workloads missing. Is this strict gate the supervisor-preferred boundary, and should the next work be an H48H11 cloud run, or should the thesis explicitly stop at "implemented exact oracle with strong empirical evidence, without an all-state practical-runtime proof"?
78. The default fast-target proof path has now been resized from H48H11 to H48H10 because H48H10 is the smallest vendored upstream 16-thread benchmark target with distance-20 and superflip timings while fitting the proposed 16 CPU / 64 GiB RAM / 250 GiB NVMe cloud machine class more plausibly. The generated H48H10 plan/runbook targets a 30,336,314,216-byte table, keeps H48H11 as an optional stronger target, and still requires trusted table generation plus all 8 fast-target cloud workloads before `fast_runtime_proven_for_every_possible_state` can become true; the current laptop preflight remains false because it has 8 logical CPUs, 16 GiB RAM, and about 20 GiB free disk. Is H48H10 acceptable as the paid/idle-machine default for attempting the fast all-state proof, or should the thesis only use H48H11 for that wording and leave H48H10 as intermediate evidence?
79. The H48H10 runbook now has a one-command `run_end_to_end_single_machine.sh` path and a worker-side `install_prerequisite_tables.sh` path backed by `scripts/install_h48_table_bundle.py`, which rejects unsafe bundles and validates table size plus SHA-256 before hard-tail search. Is this reproducibility and table-transfer evidence sufficient for running the paid/idle-machine proof campaign, or should the supervisor require additional operator checks before accepting copied H48 table artifacts?
80. The H48H10 proof can now be launched through `scripts/run_h48_fasttarget_remote.py`, which plans or executes SSH/rsync synchronization, remote `run_end_to_end_single_machine.sh`, result-archive fetch, local unpack, and final evaluation/contract/audit. The same runner records and tests a failure-diagnostic path that fetches remote `results/processed/` if a remote step fails before the normal archive is produced. It can also run the expensive H48H10 prerequisite-table stage separately with `--remote-action prerequisites`, fetch the generated prerequisite table archive and processed diagnostics, later resume the proof with `--remote-action full`, and inspect a remote proof host with read-only `--remote-action status` before or after long runs. Is this remote execution and status evidence structure sufficient for accepting a paid-machine run as reproducible proof, provided the resulting H48 contract flips `fast_runtime_proven_for_every_possible_state=true`?
81. The resident native H48 path now preserves its loaded table process when the native backend returns a structured timeout row: `results/processed/h48_resident_timeout_survival_seed_2026_thesis_h48h0_lowload.json` records a tiny-timeout superflip row followed by a verified exact `R` row in the same resident session, and the H48 contract records `python_h48_resident_timeout_keeps_loaded_process=true`. This removes an avoidable table-reload overhead class for timeout-heavy H48/H48H10 campaigns, but the contract still keeps `fast_runtime_proven_for_every_possible_state=false`. Should this be treated as sufficient local optimization before the paid H48H10 run, or should additional native H48 search-table improvements be required first?
82. The native H48 batch wrapper now preserves completed rows after an outer process timeout. `results/processed/h48_batch_partial_timeout_recovery_seed_2026_thesis_h48h0_lowload.json` simulates a batch where the first row has already printed an exact JSON solution and the second row is unfinished; the artifact records `partial_rows_preserved=true`, row statuses `exact` then `timeout`, and the H48 contract records `python_h48_batch_recovers_partial_timeout_rows=true`. Is this progress-preservation behavior enough for accepting H48H10 batch workload artifacts with partial failures, provided the final evaluator still requires every required workload row to pass before the all-state-fast claim is accepted?
83. The native H48 lower-bound batch wrapper now preserves completed admissible rows after an outer timeout and reuses them for rotational-race ordering. `results/processed/h48_lower_bound_partial_timeout_recovery_seed_2026_thesis_h48h0_lowload.json` simulates two completed lower-bound rows followed by timeout; the artifact records `partial_lower_bound_preserved=true`, `partial_ordering_preserved=true`, and the H48 contract records `python_h48_lower_bound_recovers_partial_timeout_rows=true`. Is this exact-safe partial lower-bound behavior enough for the H48H10 proof harness, or should the next paid/idle run require a live hard-tail artifact showing that lower-bound ordering actually reduces distance-20 runtime?
84. The remote H48H10 runner now has a status-aware `--remote-action resume` mode. It syncs the repo, probes the remote target table/metadata/contract status, skips `run_full_prerequisites.sh` and prerequisite archive fetches when the H48H10 table exists with the expected 30,336,314,216-byte size and trusted metadata, then runs the full proof path. The dry-run artifact `results/processed/h48_fasttarget_remote_run_cloud_20260601_h48h10_fasttarget_batch10_h48h10_resume_dryrun.json` records this 15-step conditional plan, and focused tests prove a table-ready status skips prerequisite generation. Is this restart/resume structure enough for the paid H48H10 run, or should the supervisor require a manual checkpoint between prerequisite generation and full hard-tail execution?
85. The remote H48H10 runner now requires the final regenerated H48 contract to prove the claim before the proof runner itself can pass: successful remote/fetch/finalize shell commands alone are no longer enough for `--remote-action end-to-end`, `full`, or `resume`. Is this strict final-contract gate the preferred acceptance boundary for any paid H48H10 proof artifact, or should the runner also allow a "commands passed but contract false" diagnostic success state for operator review?
86. The generated H48H10 `run_full.sh` script now performs worker preflight with `--require-target-table` and full table validation before running the full hard-tail workloads, so the standalone full-search entrypoint has the same trusted-table gate as the end-to-end, remote full/resume, and per-worker scripts. Is this sufficient operator safety for cloud execution, or should every long-running shell script also include an explicit human confirmation prompt before expensive work?
87. The universal RubikOptimal fallback now reuses the top-level resident `RubikOptimalOracleSession` across fallback rows instead of starting a separate batch-helper subprocess; focused tests and regenerated H48 contracts record `universal_oracle_rubikoptimal_fallback_uses_shared_resident_session=true`, while H48H10 remains missing the trusted table and proof workloads. Is this exact-safe optimization plus tests/contract evidence sufficient for the thesis implementation claim, or should a live H48H10/RubikOptimal hard-tail artifact be required before any stronger "fast optimal oracle for every possible 3x3 state" wording is approved?
88. The universal RubikOptimal batch prepass now also reuses the top-level resident `RubikOptimalOracleSession` instead of starting a separate batch-helper subprocess; focused tests and regenerated H48 contracts record `universal_oracle_rubikoptimal_prepass_uses_shared_resident_session=true`, while H48H10 still lacks the trusted table and proof workloads. Should the thesis present this as sufficient optimized-oracle engineering evidence, or should stronger wording remain blocked until a live H48H10/RubikOptimal hard-tail run proves the target runtime on distance-20 representatives?
89. The universal RubikOptimal sequential symmetry batch now also reuses the top-level resident `RubikOptimalOracleSession` for single-state and solve-many calls when `rubikoptimal_symmetry_max_concurrency=0`; focused tests and regenerated H48 contracts record `universal_oracle_rubikoptimal_symmetry_batch_uses_shared_resident_session=true`, while the intentionally parallel rotational race remains separate. Does this complete the local resident-session optimization story for RubikOptimal prepass/symmetry/fallback, or should stronger thesis wording still wait for a live H48H10/RubikOptimal hard-tail proof artifact?
90. The H48H10 proof route is now explicitly non-AWS by default: the AWS helper scripts are archived at the AWS CLI boundary, the single-machine runbook now finalizes by regenerating the H48H10 contract/audit after collection, and `scripts/run_h48_fasttarget_local_proof.py` can dry-run or execute the generated proof scripts on the current approved machine after scanning for AWS usage. The local wrapper now embeds the executed preflight artifact summary, so the proof-host machine gate can be audited from one wrapper JSON. The proof-host preflight separates storage class from free workspace: generated H48H10 scripts require a 250 GiB class proof volume through `--min-storage-gib 250.0`, while `target_h48_workspace` computes the actual H48H10 free-space gate. The fresh wrapper artifact fails safely on this laptop because it has 8 CPUs and 16.000 GiB RAM below the 16 CPU / 64 GiB gate, while its 37.927 GiB current H48 workspace satisfies the computed 32.491 GiB H48H10 workspace requirement. Is this corrected local/generic proof-host route the preferred operational path, and should the final claim remain blocked until that route produces the trusted H48H10 table and eight passing proof workloads?
91. The H48 contract now prefers the freshest local no-AWS stronger-table status by payload identity rather than filename text, so the current artifact records the previous H48H8 wait-safe PID `10554` as not alive, with no native H48 backend running and no trusted stronger table. Is this current negative status evidence the right boundary to show in the thesis/progress logs until a new approved proof-host run starts, rather than keeping older "PID alive" waiter snapshots in the contract?
92. The H48 contract now also records the actual H48H10 fast-target status separately from the first-missing H48H8 ladder status. The H48H10 status artifact was produced as a no-execution dry-run/status pair, records no trusted H48H10 table, no native backend running, and current local safety `false` because available memory is about 0.20 GiB below the 4.00 GiB mmap guard. Should the thesis present this as the current proof boundary and require a non-AWS proof host to generate/validate H48H10 plus all eight proof workloads before using "fast optimal oracle for every possible 3x3 state" wording?
93. The H48 worker/preflight/remote proof path now has an exact-safe persistent full-checksum certificate keyed by table and metadata identity, so a future 30.3 GiB H48H10 table is fully hashed once per unchanged copy and then reused by status/resume/workload validation without repeated full-table reads. Is this sufficient proof-host efficiency preparation before running H48H10 on the non-AWS 16 CPU / 64 GiB / 250 GiB class machine, or should additional native search/table-generation optimizations be required first?
94. The detached H48H10 prerequisite waiter now refuses `ready_for_resume=true` unless the prerequisite archive exists, the target table has the expected size, metadata says `trusted_table=true`, and the full checksum validation passes through the persistent certificate path. Is this checksum-gated resume boundary sufficient operator safety before a non-AWS H48H10 proof-host run starts the full hard-tail workloads, or should a human review checkpoint still be required after prerequisite generation and before full proof execution?
95. The H48 table-bundle installer now skips source bundle extraction/hash/copy work when the canonical target table already passes trusted metadata plus full checksum validation, and first-time archive installs hardlink from the private extracted bundle into the staged canonical table path when possible before falling back to copy. Is this enough resume/install efficiency for distributing the generated H48H10 prerequisite to workers, or should the proof-host run still include a manual operator checkpoint before installing or reusing copied table bundles?
96. The H48H10 fast-target canary/full plans and non-AWS runbook now make the native generator work-batch explicit: the shared `stronger_table_h48h10` prerequisite uses `--gendata-workbatch 256 --mmap-sync-mode async --backend-cflag=-march=native --skip-generation-distribution-scan`, and the runbook summary records `h48_gendata_workbatch=256`. Is this optimized proof-host command boundary acceptable for the 16 CPU / 64 GiB / NVMe class run, or should the supervisor require a conservative synchronous/default-generation pass before accepting any H48H10 table evidence?
97. The generic non-AWS H48H10 proof wrapper now validates the runbook and linked canary/full plans before any `--execute` run can start, including the H48H10 solver target, non-AWS flags, generated staged-proof scripts, full distance-20 hard-tail coverage, 8 required full proof workloads, and the optimized `stronger_table_h48h10` generation flags. Is this package-validation gate sufficient before launching the non-AWS proof-host run, or should there be an explicit human checkpoint after dry-run validation and before H48H10 generation begins?
98. The local non-AWS proof wrapper now applies the same package-validation gate before running generated proof scripts on the current machine. The validated local preflight artifact still fails safely because this laptop has 8 CPUs and 16 GiB RAM below the 16 CPU / 64 GiB proof-host requirement, even though the H48 workspace check has enough headroom in that snapshot. Is this enough evidence to prohibit local H48H10 generation and move the actual proof to a non-AWS proof host, or should any additional local preflight evidence be collected before the off-machine run?
99. The generated H48H10 runbook now records SHA-256, byte size, and mode for all 23 generated proof scripts, including `collect_prerequisite_table_parts.sh`, and both the generic non-AWS and local proof wrappers validate those fingerprints before any `--execute` run can start. The H48 contract also requires the non-AWS dry-run artifact to show `generated_file_fingerprint_count=23`, no missing fingerprint keys, and all script fingerprint checks passing. Is manifest-level generated-script fingerprinting sufficient proof-host package integrity for the non-AWS run, or should the supervisor require a signed archive checksum or manual review checkpoint before H48H10 generation starts?
100. The H48H10 runbook now includes `recover_prerequisite_metadata.sh`, which can adopt metadata only for an already completed canonical H48 table file and then validates the checksum through the persistent worker-table path. It explicitly refuses staged `.partial` files because mmap partial files can be full-size before native generation finishes. Is this canonical-table-only recovery acceptable after an interrupted proof-host prerequisite stage, or should any interrupted H48H10 prerequisite require manual review or full regeneration before the hard-tail proof workloads run?
101. The same canonical-table-only recovery is now exposed as `scripts/run_h48_fasttarget_remote.py --remote-action recover-prerequisite-metadata` and through the generic non-AWS proof wrapper, so a future proof host can recover interrupted prerequisite metadata through an audited dry-run/execution path rather than manual SSH. Is this first-class recovery orchestration acceptable, or should the proof protocol require an explicit human checkpoint before running recovery and continuing to H48H10 full proof workloads?
102. The native H48 short-cube generation path now uses edge-only symmetry expansion and the H48H0 checksum-equivalence smoke proves the generated table bytes are unchanged for h48h0. Is that sufficient generator-optimization evidence before attempting the H48H10 proof-host run, or should the thesis/proof package also require an H48H8/H48H10 timing comparison on the target machine before accepting the optimized generation flags?
103. The H48H10 contract now separates the table-independent fast-target proof-plan gate from actual table/runtime completion. The regenerated H48H10 artifact records `h48_capacity_fast_target_proof_plan_valid=true` and optimized ladder flags (`--gendata-workbatch 256 --skip-generation-distribution-scan --mmap-sync-mode async --backend-cflag=-march=native`), while still failing because the H48H10 table, trusted metadata, checksum evidence, and proof workloads are absent. Is this proof-plan gate sufficient before running the non-AWS proof host, or should additional local H48H8/H48H10 generation timing evidence be required first?
104. The H48H10 preflight can now record an assumed non-AWS proof-host shape without launching AWS, SSH, or heavy local generation. The saved assumed-machine artifact uses 16 CPUs, 64 GiB RAM, a 250 GiB proof volume, and 40 GiB free H48 workspace; it passes the computed H48H10 workspace gate but records `machine_source=assumed` and `fast_runtime_proven_for_every_possible_state=false`. Is this shape-only artifact sufficient to approve running the actual proof on a real non-AWS 16 CPU / 64 GiB / 250 GiB-class host, or should a live preflight from that host be required before H48H10 generation starts?
105. The H48H10 proof package now supports split-manifest H48 table bundles: the source table must pass full trusted checksum validation before splitting, each part is SHA-256 checked, the worker installer validates and assembles the parts, and the canonical installed table is still full-checksum certified before proof workloads. Is this acceptable for distributing a generated 30.3 GiB H48H10 prerequisite across non-AWS proof workers, or should the supervisor require a single archive, signed external checksum, or manual review checkpoint before worker installation?
106. The generic non-AWS H48H10 proof runner now supports three prerequisite bundle modes: `archive`, `split`, and `both`. Split mode avoids the single large tarball by fetching `cloud_hardtail_prerequisite_tables_<run>_parts/`; both mode keeps a tarball fallback while installing from split parts. Should the approved proof-host run use split mode only, both formats for redundancy, or the legacy archive route for review simplicity?
107. The local split-bundle smokes now prove the split-manifest path on both trusted h48h0 and oracle-grade h48h7 tables. `results/processed/h48_split_bundle_smoke_seed_2026_thesis_h48h0_local.json` records four checked h48h0 parts and full post-install checksum validation; `results/processed/h48_split_bundle_smoke_seed_2026_thesis_h48h7_oracle_grade_local.json` records four checked h48h7 parts, `oracle_grade=true`, source/installed size 3,793,842,344 bytes, matching checksums, and full post-install checksum validation. Is this sufficient package-level evidence before applying the same split path to a future H48H10 proof-host table, or should an external signed checksum review or manual operator checkpoint still be required first?
108. The H48H10 non-AWS proof package now has a single byte-bound manifest: `results/processed/h48_fasttarget_nonaws_proof_package_seed_2026_thesis_h48h10_nonaws_splitbundle_validated.json` fingerprints the runbook, canary/full plans, assumed proof-host preflight, and generated proof scripts; records the current H48H10 contract gap summary separately; records the exact split-mode 15-step generic SSH plan; scans for AWS usage; records `full_plan_summary.required_workload_count=8`; and has package SHA-256 `d6f272de8dbe9c9fdb7a17fde1ea1c7673a7d6997ced29665d8ce56964a2bd07`. Is this byte-bound package manifest sufficient approval evidence before launching the actual non-AWS proof-host run, or should a live preflight artifact from the target machine be required first?
109. The H48H10 proof-package builder now enforces that distinction: the default artifact is only `package_mode=planning` with `launchable_for_execution=false`, and `--require-live-preflight` refuses to pass from the assumed 16 CPU / 64 GiB / 250 GiB artifact. Should the approved run protocol require a passing live target-host preflight (`machine_source=local`, not assumed) and a launchable proof-package artifact before any H48H10 table generation starts?
110. The H48 table byte root is now configurable through `RUBIK_OPTIMAL_H48_TABLE_ROOT`, and the trusted metadata/checksum contract, capacity/preflight disk checks, cloud expected-artifact paths, runbook metadata recovery, and remote status/wait snippets all use that configured root. Is the approved H48H10 proof protocol allowed to place the 30.3 GB table on an external/NVMe proof volume or non-AWS host volume through this environment variable, provided a live preflight against that configured root passes before generation and the final H48H10 contract still requires trusted metadata, full checksum, and all eight proof workloads?
111. The new local proof-volume inspector `results/processed/h48_proof_volume_candidates_seed_2026_thesis_h48h10_local_noaws_current.json` records zero launchable H48H10 candidates on this laptop: the host has 8 CPUs and 16.000 GiB RAM against the 16 CPU / 64 GiB gate, about 0.167 GiB immediately available memory against the 4 GiB mmap guard, and no mounted candidate root that makes the full host/volume gate launchable. Should the approved H48H10 run protocol require this same inspector to pass on the target non-AWS machine, in addition to `cloud_hardtail_preflight.py`, before any H48H10 generation begins?
112. The H48H10 proof package now enforces that exact boundary: planning mode can pass while recording `launchable_for_execution=false`, but launchable mode requires both `preflight_is_live_runtime_evidence=true` and `proof_volume_report_launchable=true`. The refreshed planning package has SHA-256 `e617d0b7dd3101ae0a43fd1fac3b8b2f9f1ee893bc3fe544ae5f68cdeb6803bd`, records the current proof-volume report with zero launchable candidates, and the launchable refusal artifact records `proof_volume_requirement_satisfied=false`. Is this two-gate launch protocol sufficient before starting H48H10 generation on a non-AWS proof host, or should a manual supervisor/operator checkpoint still be required after both gates pass?
113. The local non-AWS proof wrapper now has `--action staged-proof`, which expands the generated H48H10 `single_machine_run_order`, validates all generated-script fingerprints, scans every step for AWS usage, records per-step results, stops at the first failed gate, and still requires the final H48H10 contract to prove `fast_runtime_proven_for_every_possible_state=true`. Is this staged local sequence runner the preferred one-machine protocol for an approved non-AWS proof host, or should the run still use the generated shell `run_end_to_end_single_machine.sh` directly with a separate manual checkpoint before the expensive prerequisite generation step?
114. The staged local proof wrapper now refuses `--action staged-proof --execute` before any generated step runs unless the proof package is explicitly launchable: `package_mode=launchable`, `readiness_classification=launchable_nonaws_proof_package`, live preflight required and satisfied, proof-volume report required and launchable, and package-level preflight/proof-volume checks true. The current real refusal artifact records `execute=true`, `command_result=null`, `step_results=[]`, and refusal against the default planning package. Should this launchable-package gate be the final automated start condition for the approved non-AWS proof host, or should a manual supervisor/operator checkpoint still be required immediately before H48H10 prerequisite generation begins?
115. The generic SSH/non-AWS proof wrapper now applies the same launchable-package gate before execute-mode actions that can start H48H10 prerequisite generation, full proof work, canary execution, resume, or staged proof execution. The current real refusal artifact records `requested_execute=true`, `execute=false`, `planned_step_count=15`, no `underlying_remote_artifact`, and refusal before SSH/rsync because the default package remains planning-only. Should this generic-runner gate be considered sufficient protection for the actual off-laptop proof route, or should the approved protocol require a separate manual sign-off even after a launchable package is generated on the target host?
116. The new `scripts/prepare_h48_fasttarget_nonaws_launch.py` command produces one audited launch-preparation artifact by running live preflight, proof-volume inspection, and launchable-package construction without starting H48 generation or proof workloads. The current laptop artifact is correctly `not_launchable`; on a real non-AWS proof host, should a passing artifact from this script be the automated start condition for H48H10 prerequisite generation, or should the approved run protocol still require a separate manual supervisor/operator checkpoint after that artifact passes?
117. The generic SSH/non-AWS proof wrapper now revalidates `package_sha256` and every stored `component_fingerprint` in a launchable package immediately before execute-mode H48H10 actions; a regression mutates a fingerprinted live-preflight artifact after package creation and the wrapper refuses without remote execution. Is this automated byte revalidation sufficient proof-package integrity for the non-AWS run, or should the approved protocol still require a manually signed archive/checkpoint before H48H10 prerequisite generation starts?
118. The generic SSH/non-AWS proof wrapper now writes a local pre-remote checkpoint after all execute-mode launch guards pass and before `run_remote_fasttarget_proof` starts; it records planned steps, guard evidence, and explicit resume/status commands so a detached H48H10 proof can be recovered if the local controller dies. Is this automated recoverability checkpoint sufficient for the approved non-AWS proof-host run, or should a human checkpoint be required immediately before the first remote detached prerequisite starts?
119. The H48 parallel rotational race now clips each native child's `--search-timeout-ms` to the remaining global race budget, so later rotation waves cannot consume a full original timeout after most of the phase budget has elapsed. Is this timeout discipline enough before the approved H48H10 proof-host run, or should additional native hard-tail optimizations be required before generating the 30.3 GiB H48H10 table and running the eight proof workloads?
120. The first own-code Path 2 slice used only the repository's 24 proper whole-cube rotations to prune equivalent native Korf root moves, reducing the canonical superflip first-move representatives from 18 to 3 and preserving exactness on shallow BFS-checked symmetric states. Now that the later full-48 slice has superseded this as a stronger measurement, should thesis wording keep the 24-rotation result as a historical root-level rotational pruning checkpoint, or omit it in favor of the full-symmetry negative result?
121. The follow-up own-code Path 2 slice now adds opt-in 24-rotation canonicalization for native Korf transposition-table keys (`--symmetry-transpositions`) and passes BFS/native-focused tests after disabling the non-rotation-invariant opposite-face ordering prune in that mode, but measured superflip probes still time out at `final_bound=16`; the canonical table increases transposition hits and reduces generated nodes locally, but not enough to change the worst-case proof boundary. Should thesis wording present this as an experimental exact-safe rotational duplicate-pruning result with negative worst-case evidence, and require a true symmetry-reduced coordinate/heuristic or phase/coset proof before making any fast own-code superflip claim?
122. The larger edge-projection probe now shows why a new Path 2 table must be symmetry-reduced rather than raw: orientation-only and orientation/slice projections keep superflip at 7-8, prefix 8-edge full projection still gives distance 8, but prefix 9-edge full projection gives projected distance 11 over `40,874,803,200` raw states. Should the next own-code heuristic attempt be framed as a symmetry-reduced 9-edge PDB/quotient-table experiment, with the raw 9-edge table explicitly rejected as impractical for the 8-core/16 GiB laptop?
123. The native sparse edge-projection follow-up validates the 9-edge result and proves stronger prefix 10-edge and 11-edge projections have superflip projected distance greater than 12, but those raw spaces are `245,248,819,200` and `980,995,276,800` states respectively, and the sparse probes are not runtime heuristics. Should the next own-code heuristic be framed as a compressed/symmetry-reduced larger-edge threshold or quotient table, rather than a full byte PDB, and is a 13+ projected lower-bound target sufficient to justify that implementation risk?
124. The rotation-orbit sweep sharpens question 123: 9-edge has 13 rotation representatives and tops out at projected distance 12, while all five 10-edge representatives are exactly projected distance 13 from solved to superflip. Should the thesis frame 10-edge, not 9-edge, as the smallest measured Path 2 heuristic target, and is a compressed/symmetry-reduced 10-edge threshold table a defensible next own-code implementation step even though a raw one-bit 10-edge table would be about 30.7 GB?
125. The 10-edge threshold-table feasibility probe rejected the simplest compression path before the full-48 run: the raw fixed-subset solved-side ball reaches the 50,000,000-state cap before completing depth 7, and the 24-rotation canonical quotient still completes depth 7 only at 47,030,192 states with a 43,206,124-state frontier in 607.726s. Should this be kept as negative evidence for raw/24-rotation tables, now superseded by question 126 for the full-symmetry result, or should only the strongest full-48 negative result be discussed with the supervisor?
126. The orientation-reversing half of Path 2 has now been implemented and measured: full 48-symmetry root pruning reduces canonical superflip first-move representatives to `U,U2`, the 10-edge depth-7 quotient drops from 47,030,192 states under 24 rotations to 23,517,154 states under all 48 symmetries, but native Korf superflip probes with 24-rotation and 48-symmetry transposition tables still time out at `final_bound=16` in the saved 20s/60s artifacts. Should further own-code effort now move to a stronger quotient/compression or native phase/coset proof layer, or should this full-symmetry negative evidence be enough to stop the own-code hardest-state track and rely on the attributed H48/Nissy oracle for distance-20 certification?
127. The compact exact transposition-table follow-up removes the native Korf capacity-skip bottleneck at 32M entries and uses a saved verified length-20 superflip solution only as an upper bound, but the own-code proof still times out at bound 19 in `results/processed/native_superflip_upper_bound_probe_seed_2026_compact20.json` (`native_optimality_proved=false`). Should further own-code effort now prioritize a stronger admissible 10-edge quotient/threshold heuristic or native phase/coset proof, rather than more transposition-table engineering, or should the thesis stop the own-code hardest-state track here and present H48/Nissy as the attributed distance-20 certification lane?
128. The native Kociemba phase/coset route now moves both phases into own-code C++ and has a corrected fixed-axis proof boundary: full whole-cube root representatives `U,U2` are too broad for Kociemba's UD-axis G1 target, so the current proof wrapper uses the G1-preserving superflip mask `U,U2,R,R2`. Pair pruning plus no-dedup/8-thread search finds and independently verifies a length-20 superflip solution in 0.66088s, but the strict target-19 proof still times out after 600.043s after completing phase-1 depth 15 and entering depth 16 (`proves_no_solution_at_or_below_target=false`). A serial corner-permutation target-set lower bound prunes 2,311,094,861 branches in 600s but still does not finish depth 16, while the labeled UD-slice target projection pruned zero branches in the 60s superflip row. Should further own-code effort now focus on a stronger full-state target-set/reverse pruning layer or phase/coset quotient to prove no length-19 solution, or should the own-code hardest-state track stop at a verified length-20 solution while H48/Nissy remains the attributed optimality proof?
129. The stronger combined CP x labeled-UD-slice target-set table is now implemented as an opt-in cached native layer and measured on the exact depth-16 target-19 superflip bottleneck. The cached row loads a 457 MiB complete cap-3 table in 1.52082s, records max table distance 8, prunes 122,182,461 combined branches, and cuts phase-2 handoffs to 2,231,322, but still times out after 600.001s with `phase1_start_depth=16` and `proves_no_solution_at_or_below_target=false`. Is this enough negative evidence to stop adding separable/low-dimensional target projections and either attempt a more ambitious full-state/quotient reverse layer or keep the own-code hardest-state result as verified length-20 only?
130. The latest threaded target-pruning follow-up adds explicit threaded CP/CP x labeled-slice target-table use plus a labeled UD-edge target projection. The depth-17-only target-19 row builds a complete cap-2/depth-17 UD-edge target table in 1.92178s, prunes 27,382,581 UD-edge branches plus 1,199,973,642 CP x slice branches, cuts phase-2 handoffs to only 543, and has no phase-2 timeout rows, but still times out after 901.249s with `completed_phase1_depth=-1` and `proves_no_solution_at_or_below_target=false`. Is this now sufficient evidence that the remaining own-code proof barrier is phase-1 enumeration/quotienting rather than phase-2 suffix proof, and should future work stop adding low-dimensional target projections unless they directly reduce the phase-1 frontier enough to complete target 19?
131. After re-reading the topic brief and comparing against the delivered-reference thesis structure, the defensible scope boundary appears to be: cite God's Number/superflip distance 20 from established literature and attributed H48/Nissy evidence, report the student's native Kociemba path as a fast verified length-20 superflip solution, and leave the native proof that no length-19-or-shorter superflip solution exists as future work unless a later own-code proof completes. Is this boundary acceptable for supervisor review, or must the thesis continue attempting the optional own-code no-19 proof before submission?

## Thesis scale

1. Should the final thesis target roughly 90-120 pages, matching the delivered example's scale?
2. Is a 22,000-30,000 word Greek body appropriate for this topic?
3. Should generated code listings be placed in appendices or omitted in favor of repository references?
4. How many implementation screenshots/diagrams are expected versus pure algorithmic tables/plots?

## AI assistance

1. What disclosure is required for use of Codex or other AI tools?
2. Are AI-generated drafts allowed if the student verifies code, sources, and final text?
3. Should AI assistance be mentioned in acknowledgements, appendix, or a formal declaration?
4. Can the approval be recorded in `docs/final_supervisor_approval.md` with source/date and explicit approval of AI disclosure, front-matter style, bibliography style, and scoped solver claims?

## Results and evaluation

1. Is it acceptable to report timeouts for hard optimal-search cases, even when a separate native optimal evidence path proves some deeper selected cases exactly?
2. Should the thesis include stress-run data if it is not part of the reproducible quick profile?
3. Is publication-level novelty expected, or is implementation/comparison sufficient?
4. Should all generated data be committed, or should large tables be regenerated from scripts during review?
